# 滚动抽取修复说明

## 问题描述

用户反馈在抽取时，变蓝的那一行没有固定在中间位置，并且希望只显示三行数据。

## 修复内容

### 1. CSS样式修改

#### 滚动列表容器
- 将 `overflow-y: auto` 改为 `overflow: hidden`
- 将 `justify-content: center` 改为 `justify-content: flex-start`
- 隐藏滚动条：`display: none`

#### 滚动条样式
- 移除所有滚动条相关的样式
- 确保只显示3行数据，不显示滚动条

### 2. JavaScript逻辑修改

#### 创建滚动列表 (`createRollingList`)
- 设置容器高度为3行数据的高度：`containerHeight = visibleItems * itemHeight`
- 限制滚动列表显示区域，只显示3行
- 创建列表后立即高亮中间行

#### 滚动动画 (`startRolling`)
- 中间行始终高亮：`highlightCenterRow()`
- 调整滚动位置，确保显示完整的3行数据
- 使用 `Math.floor(currentScrollTop / itemHeight) * itemHeight` 确保滚动位置对齐

#### 中间行高亮 (`highlightCenterRow`)
- 新增函数，始终高亮第2行（索引为1）
- 中间行固定位置，不随滚动变化
- 清除之前的高亮状态

### 3. 核心改进

#### 固定显示3行
- 滚动列表容器高度限制为3行
- 隐藏滚动条，提供更清晰的视觉效果
- 确保用户始终看到3行数据

#### 中间行固定高亮
- 中间行（第2行）始终显示为蓝色高亮
- 不随滚动位置变化而改变高亮位置
- 提供稳定的视觉焦点

#### 滚动逻辑优化
- 滚动时保持3行数据的完整显示
- 滚动位置对齐到行边界
- 停止时确保中间行高亮

## 技术实现

### 关键函数

1. **`createRollingList`**: 创建固定高度的滚动列表
2. **`highlightCenterRow`**: 高亮固定的中间行
3. **`startRolling`**: 滚动动画，保持中间行高亮
4. **`clearCenterHighlight`**: 清除高亮状态

### CSS类名

- `.rolling-list`: 滚动列表容器
- `.rolling-item`: 滚动项目
- `.center-highlight`: 中间行高亮样式

## 效果说明

### 修改前
- 滚动列表显示多行数据
- 高亮行位置不固定
- 显示滚动条
- 中间行高亮位置随滚动变化

### 修改后
- 滚动列表只显示3行数据
- 中间行始终高亮且位置固定
- 隐藏滚动条
- 提供更清晰的视觉体验

## 使用说明

1. 启动抽取动画后，滚动列表将显示3行供应商数据
2. 中间行始终显示为蓝色高亮
3. 滚动过程中保持3行数据的完整显示
4. 停止时中间行保持高亮状态

## 注意事项

1. 确保供应商数据足够多，以支持滚动效果
2. 中间行高亮样式通过CSS和JavaScript共同控制
3. 滚动动画性能已优化，支持流畅的滚动效果
4. 响应式设计保持兼容性

## 未来改进

1. 可考虑添加滚动速度控制
2. 支持自定义显示行数
3. 添加更多动画效果
4. 优化大数据量时的性能

