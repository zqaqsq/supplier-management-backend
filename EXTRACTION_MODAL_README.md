# 智能抽取弹窗使用说明

## 概述
本项目已成功将抽取界面改造成弹窗形式，用户点击抽取按钮后，`extraction-modal.html` 作为弹窗弹出，实现了图片中显示的智能抽取功能。

## 功能特性

### 🎯 智能抽取配置
- **抽取数量**: 支持3-5家供应商的抽取
- **资质等级**: 可选择全部或特定等级（A级、B级、C级、D级）
- **行业筛选**: 支持制造业、建筑业、服务业、贸易业等行业筛选
- **操作人**: 显示当前操作人员（默认：张三）

### 🚀 智能抽取算法
- 使用加权随机算法进行智能抽取
- 考虑供应商资质等级、地区分布、行业多样性等因素
- 自动过滤经营状态异常的供应商
- 确保抽取结果的公平性和合理性

### 📊 抽取结果展示
- 美观的卡片式结果展示
- 显示供应商的详细信息（ID、资质、行业、地区等）
- 支持重新抽取、导出结果、打印结果等操作

## 使用方法

### 1. 触发弹窗
在主页面（`index.html`）中，点击"智能抽取"按钮即可打开抽取弹窗：
```html
<button type="button" class="btn btn-success me-2" onclick="showExtractionModal()">
    <i class="bi bi-shuffle me-1"></i>智能抽取
</button>
```

### 2. 配置抽取参数
在弹窗中配置以下参数：
- **抽取数量**: 选择要抽取的供应商数量（3-5家）
- **资质等级**: 选择供应商的资质等级要求
- **行业**: 选择供应商的行业类别
- **操作人**: 确认操作人员信息

### 3. 执行抽取
点击"开始智能抽取"按钮，系统将：
1. 根据配置条件过滤供应商
2. 使用加权算法进行智能抽取
3. 显示抽取结果
4. 自动保存抽取记录到数据库

### 4. 结果操作
抽取完成后，可以：
- **重新抽取**: 返回配置界面重新设置参数
- **导出结果**: 将结果导出为CSV文件
- **打印结果**: 打印抽取结果报告

## 技术实现

### 前端架构
- **弹窗容器**: 使用iframe加载抽取弹窗
- **响应式设计**: 支持不同屏幕尺寸的设备
- **现代化UI**: 使用Bootstrap和自定义CSS实现美观界面

### 后端集成
- **API接口**: 通过`/api/suppliers/all`获取供应商数据
- **数据保存**: 抽取结果自动保存到`/api/selection-results`
- **错误处理**: 包含完整的错误处理和回退机制

### 通信机制
- **父子页面通信**: 使用postMessage进行跨页面通信
- **弹窗控制**: 支持键盘ESC键和点击遮罩关闭弹窗
- **状态同步**: 弹窗状态与父页面保持同步

## 文件结构

```
src/main/resources/static/
├── index.html              # 主页面，包含抽取按钮
├── extraction-modal.html   # 抽取弹窗页面
└── js/                     # JavaScript文件目录
```

## 配置说明

### 抽取数量限制
- 最小值：3家
- 最大值：5家
- 系统会根据符合条件的供应商数量自动调整

### 资质等级权重
- A级：权重1.0（最高优先级）
- B级：权重0.8
- C级：权重0.6
- D级：权重0.4（最低优先级）

### 行业筛选
支持以下行业类别：
- 制造业
- 建筑业
- 服务业
- 贸易业
- 其他

## 使用示例

### 基本抽取流程
1. 用户点击"智能抽取"按钮
2. 弹窗显示配置界面
3. 用户设置抽取参数
4. 点击"开始智能抽取"
5. 系统执行抽取算法
6. 显示抽取结果
7. 用户选择后续操作

### 高级功能
- **条件组合**: 可以同时设置资质等级和行业条件
- **智能回退**: 如果条件过于严格，系统会给出提示
- **结果持久化**: 所有抽取结果都会保存到数据库
- **操作日志**: 记录完整的抽取操作过程

## 注意事项

1. **浏览器兼容性**: 建议使用现代浏览器（Chrome、Firefox、Safari、Edge）
2. **网络要求**: 需要稳定的网络连接以访问后端API
3. **数据完整性**: 确保数据库中有足够的供应商数据
4. **权限控制**: 当前版本使用默认用户，实际部署时需要添加用户认证

## 扩展功能

### 未来计划
1. **用户认证**: 集成Spring Security实现用户登录
2. **抽取历史**: 查看和管理历史抽取记录
3. **规则配置**: 支持自定义抽取规则
4. **实时通知**: 添加WebSocket支持实时状态更新
5. **数据分析**: 提供抽取结果的统计分析功能

### 自定义开发
开发者可以根据需要：
- 修改抽取算法和权重计算
- 添加新的筛选条件
- 自定义结果展示样式
- 扩展导出和打印功能

## 总结

通过本次改造，项目成功实现了：
- ✅ 弹窗形式的抽取界面
- ✅ 完整的智能抽取功能
- ✅ 美观的现代化UI设计
- ✅ 稳定的前后端集成
- ✅ 良好的用户体验

用户现在可以通过简单的点击操作，在弹窗中完成所有抽取相关的配置和操作，大大提升了使用体验和操作效率。
