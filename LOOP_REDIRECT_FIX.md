# 循环跳转问题修复说明

## 问题描述
用户反馈系统存在"反复跳转"的问题，导致页面无法正常访问和使用。

## 问题分析
经过代码分析，发现以下导致循环跳转的主要原因：

### 1. WebConfig配置问题
- 使用 `addRedirectViewController` 进行重定向，可能导致循环重定向
- 根路径 `/` 重定向到 `/login.html`，在某些情况下可能触发循环

### 2. 前端登录状态检查逻辑问题
- `login.html` 中的 `checkLoginStatus()` 函数在页面加载时自动验证token
- 页面可见性变化监听器 (`visibilitychange`) 每次页面重新获得焦点都会检查登录状态
- 定时器每分钟检查一次登录状态，频率过高

### 3. 页面跳转逻辑问题
- 登录成功后立即跳转，没有防重复跳转机制
- 页面关闭时清除登录状态，导致用户需要重复登录

## 修复方案

### 1. 优化WebConfig配置
```java
// 修改前：使用redirect可能导致循环
registry.addRedirectViewController("/", "/login.html");

// 修改后：使用forward避免循环重定向
registry.addViewController("/").setViewName("forward:/login.html");
```

### 2. 优化前端登录状态检查
- 移除 `checkLoginStatus()` 函数
- 移除页面可见性变化监听器
- 降低定时检查频率：从每分钟改为每5分钟
- 添加防重复跳转逻辑

### 3. 改进页面跳转逻辑
- 登录页面加载时检查是否已登录，避免重复跳转
- 使用 `window.location.replace()` 替代 `window.location.href`
- 页面关闭时不清除登录状态，保持用户登录状态

## 修复后的效果

1. **消除循环跳转**：使用 `forward` 替代 `redirect`，避免浏览器层面的重定向
2. **减少不必要的API调用**：降低登录状态检查频率，减少服务器压力
3. **提升用户体验**：避免重复登录，保持用户登录状态
4. **增强系统稳定性**：添加防重复跳转机制，避免异常情况下的无限跳转

## 测试建议

1. 清除浏览器缓存和localStorage
2. 访问根路径 `/`，应该直接显示登录页面
3. 登录成功后应该正常跳转到主页面
4. 刷新页面或重新访问应该保持登录状态
5. 关闭页面重新打开应该不需要重新登录

## 注意事项

1. 如果用户主动登出，会清除所有认证信息
2. Token过期后会自动跳转到登录页面
3. 网络异常时不会立即跳转，避免误判
4. 建议在生产环境中配置适当的Token过期时间


