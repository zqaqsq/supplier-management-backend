# 快速修复登录问题指南

## 🚨 立即尝试的解决方案

### 方案1：强制刷新页面（推荐首先尝试）
1. 在浏览器中按 `Ctrl + F5` 强制刷新
2. 或者按 `F12` 打开开发者工具，右键刷新按钮选择"清空缓存并硬性重新加载"

### 方案2：清除浏览器存储
1. 按 `F12` 打开开发者工具
2. 切换到 "Application" 或 "存储" 标签
3. 在左侧找到 "Local Storage"
4. 右键点击域名，选择 "Clear" 或 "清除"
5. 刷新页面

### 方案3：使用测试页面诊断
1. 访问 `http://localhost:8080/test-minimal.html`
2. 查看页面显示的状态信息
3. 点击按钮测试各项功能

## 🔍 问题诊断步骤

### 步骤1：检查后端服务
确认后端服务正在运行：
- 访问 `http://localhost:8080` 应该能看到页面
- 如果无法访问，需要启动后端服务

### 步骤2：测试登录功能
1. 访问 `http://localhost:8080/login.html`
2. 使用账号：`admin` / `admin123`
3. 点击登录，观察是否跳转

### 步骤3：检查主页跳转
1. 登录成功后应该自动跳转到 `index.html`
2. 如果跳转失败，检查浏览器控制台错误信息

## 🛠️ 常见问题解决

### 问题：登录后不跳转
**解决**：检查浏览器控制台，确认跳转代码执行

### 问题：刷新后仍显示主页面
**解决**：清除 localStorage 或强制刷新

### 问题：页面显示空白
**解决**：检查 JavaScript 错误，确认所有函数已定义

## 📱 测试页面说明

### test-minimal.html
- 最小化的登录测试页面
- 显示当前登录状态
- 可以手动测试各项功能

### test-login-simple.html
- 完整的登录功能测试页面
- 详细的调试信息
- 逐步验证每个环节

## 🚀 预期结果

### 正常情况
- 未登录用户访问 `index.html` → 自动跳转到 `login.html`
- 登录成功 → 自动跳转到 `index.html`
- 已登录用户刷新 → 正常显示主页面

### 异常情况
- 显示错误信息，不跳转
- 页面空白或显示异常

## 📞 如果问题仍然存在

请提供以下信息：
1. 浏览器控制台的错误信息
2. 使用的浏览器版本
3. 具体的操作步骤
4. 测试页面的显示结果

## 🔧 技术细节

- 登录检查在 `index.html` 的 `checkLoginStatus()` 函数中
- 使用 `localStorage` 存储登录状态
- 页面加载时自动检查登录状态
- 未登录时自动跳转到 `login.html`
